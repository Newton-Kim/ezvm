all: ../../share/doc/ezvm
	cd ../.. && doxygen

local: ../../share/ezvm

../../share/ezvm: ../../share
	cp -R ezvm $<

../../share ../../share/doc/ezvm:
	mkdir -p $@

install: $(INST_DIR)/share/ezvm

$(INST_DIR)/share/ezvm:
	install -d $@
	install ezvm/* $@

clean:

distclean:
	rm -f Makefile
